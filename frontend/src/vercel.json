# Corrigir o conteúdo do arquivo vercel.js
corrected_vercel_config = '''{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}
'''

# Escrever a correção no arquivo
with open(vercel_config_path, "w", encoding="utf-8") as file:
    file.write(corrected_vercel_config)

# Confirmar que a atualização foi feita
corrected_vercel_config
